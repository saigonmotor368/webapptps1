# URL gốc của web chính TPS1 (Next.js) — nơi thực sự chứa toàn bộ API
# /api/customer/**. App này KHÔNG tự có backend riêng, chỉ là 1 frontend gọi
# sang web chính. Để trống (dùng "/api" qua proxy) khi chạy dev cục bộ.
VITE_API_BASE_URL=https://thucphamsomot.vn
